const Heading = ({ heading }: { heading: string }) => {
    return (

        <h1 className="text-white text-center mb-10 text-2xl border-2 border-white p-3 rounded-xl">{heading}</h1>

    )
}

export default Heading;